describe('diceRoll', function() {
    it("does not return a number above 6", function() {
      expect(diceRoll()).to.not.equal(7);
   });
});

// describe('Player', function() {
//     it("returns the player's score", function() {
//       var player1 = new Player();
//       expect(player1.tempScore).to.equal(0);
//       expect(player1.totalScore).to.equal(0);
//    });
// });

describe('tempDice', function() {
    it("adds the result of the dice roll to the temporary score", function() {
      expect(player1.tempScore).to.equal(???);
   });
});
